import React from 'react';
const ComponentExample = () =>{
    return(
        <>
            <h2>NPX: node pakeage execute</h2>
            <h2>NPM: node package module</h2>
            <p>component: There are like function that return HTML elements. </p>
            <p>there are teo type of component Class (it is static but you can chage data also in run time in this) and Funtion(it is dynamic)</p>
        </>
    );
};

export default ComponentExample;